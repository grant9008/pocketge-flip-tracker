package okhttp3;

/**
 * Compile-only stub of okhttp3.HttpUrl.
 *
 * Upstream (square/okhttp 4.12.0) this is Kotlin. The plugin uses exactly one
 * member, the deprecated-but-present static:
 *   okhttp/src/main/kotlin/okhttp3/HttpUrl.kt:1666
 *   fun get(url: String): HttpUrl = url.toHttpUrl()
 * which is non-null and unchecked, so neither @Nullable nor a throws clause.
 */
public class HttpUrl
{
	public static HttpUrl get(String url)
	{
		throw new UnsupportedOperationException("stub");
	}
}
